import java.util.ArrayList;

class Solution {
    public String solution(String my_string, int n) {
        int len = my_string.length();
        
        String subStr = my_string.substring(0, n);

        return subStr;
    }
}